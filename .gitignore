# PlatformIO
apps/camera-vision/firmware/.pio/

# Python
__pycache__/
*.pyc
.venv/

# Secrets — root .env (firmware creds) + every app's local .env
.env
**/.env

# Fleet definition (SSH targets/hosts) — copy fleet.example.json -> fleet.json
**/fleet.json

# AI model weights (auto-downloaded) + inference outputs
*.pt
runs/