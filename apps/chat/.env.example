# chat config. Copy to .env (gitignored).

# The home LLM cluster's single endpoint (HAProxy on the Mini PC, native Ollama API).
# Used for /api/tags (model list) and /api/chat (streaming).
LLM_BASE_URL=http://192.168.1.111:11434
