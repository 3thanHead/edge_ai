# chat config. Copy to .env (gitignored).

# The LLM cluster's single endpoint (HAProxy, native Ollama API), used for
# /api/tags (model list) and /api/chat (streaming). When you launch via
# `edge up chat` this is injected from fleet.json (run `edge fleet` to set the
# master IP); override here only for a standalone run.
LLM_BASE_URL=http://host.docker.internal:11434

# Per-node `name=url` pairs (name = hostname): unions the model list across nodes and
# enables the node picker (pin a chat to one node). Injected from fleet.json by
# `edge up chat`; set it here only for a standalone run. Empty => no picker.
# CLUSTER_NODES=nano=http://192.168.1.11:11434,macbook=http://192.168.1.12:11434
