# lead-gen config. Copy to .env (gitignored).

# The home LLM cluster's single endpoint (HAProxy on the Mini PC, OpenAI-compatible).
LLM_BASE_URL=http://192.168.1.111:11434/v1
LLM_MODEL=llama3.2:3b
# Ollama ignores the key, but the OpenAI client requires one to be set.
LLM_API_KEY=ollama
